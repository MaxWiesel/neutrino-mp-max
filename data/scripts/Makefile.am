installdir = $(bindir)

install_SCRIPTS = \
	backup.sh \
	restore.sh \
	wakeup.sh \
	install.sh 

install_SCRIPTS += \
	start_flash.sh \

if BOXTYPE_ARMBOX
install_SCRIPTS += \
	restore_flash.sh
endif
